apiVersion: v1
kind: Service
metadata:
  name: snowflake-service
spec:
  selector:
    app: snowflake-id-generator
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
